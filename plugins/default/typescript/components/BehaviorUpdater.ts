import Behavior from "./Behavior";
import { BehaviorConfigPub } from "../componentConfigs/BehaviorConfig";

export default class BehaviorUpdater {
  constructor(client: SupClient.ProjectClient, behavior: Behavior, config: BehaviorConfigPub) { /* Ignore */ }
  destroy() { /* Ignore */ }
}
